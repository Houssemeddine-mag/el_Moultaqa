# ElMoultaqa Admin

A React + Vite admin dashboard scaffold for ElMoultaqa.

- Config-driven branding is stored in `src/adminConfig.js`.
- Includes placeholder pages for dashboard, events, users, and settings.
- Run with `npm install` and `npm run dev`.
