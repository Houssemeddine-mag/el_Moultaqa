# ElMoultaqa Web App

A React + Vite attendee portal scaffold for ElMoultaqa.

- Configuration is driven by `src/conferenceConfig.js`.
- Includes pages for home, schedule, speakers, ratings, and notifications.
- Run with `npm install` and `npm run dev`.
