# ElMoultaqa Mobile

A Flutter mobile scaffold for ElMoultaqa.

- Uses a config-driven `MobileConfig` object.
- Includes a home screen, schedule preview, and announcements list.
- Run with `flutter run` after installing Flutter.
